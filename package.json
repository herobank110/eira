{
  "name": "eira",
  "version": "1.0.0",
  "description": "cardiff landmarks map + rss",
  "scripts": {
    "start": "parcel --cache-dir=build/cache --dist-dir=build/src src/index.html",
    "build": "parcel build --cache-dir=build/cache --dist-dir=build/src --public-url=./build/src src/index.html"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "google-maps": "^4.3.3",
    "parcel": "^2.4.0",
    "typescript": "^4.6.3"
  }
}
